const { userInfo } = require('os');

const router = require('express').Router();

router.get("/usertests",(req, res)=>{
    res.send("user test")
});

lh:5000/api/userInfo, r


module.exports = router